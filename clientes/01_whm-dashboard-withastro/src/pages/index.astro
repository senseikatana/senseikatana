---
import DashboardLayout from '../layouts/DashboardLayout.astro';
---

<DashboardLayout title="Panel General">
  <a slot="header-actions"
      href="#"
      class="rounded-md bg-gray-900 dark:bg-indigo-600 px-4 py-2 text-sm font-medium text-white transition hover:bg-gray-800 dark:hover:bg-indigo-700"
      data-i18n="New report" data-en="New report" data-es="Nuevo reporte"
    >
      New report
    </a>

  <div class="grid grid-cols-1 gap-4 sm:grid-cols-3">
      <article class="flex flex-col gap-4 rounded-lg border border-gray-200 dark:border-gray-800 bg-white dark:bg-gray-900 p-6">
        <div class="inline-flex gap-2 self-end rounded-sm bg-green-100 dark:bg-green-900/30 p-1 text-green-600 dark:text-green-400">
          <svg aria-hidden="true" xmlns="http://www.w3.org/2000/svg" class="size-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6" />
          </svg>
          <span class="sr-only">Increase: </span>
          <span class="text-xs font-medium" id="dashboard-revenue-change">12.4%</span>
        </div>
        <div>
          <strong class="block text-sm font-medium text-gray-600 dark:text-gray-400" data-i18n="Monthly revenue" data-en="Monthly revenue" data-es="Ingresos mensuales">Monthly revenue</strong>
          <p>
            <span class="text-2xl font-medium text-gray-900 dark:text-white" id="dashboard-revenue">$48,204</span>
            <span class="text-xs text-gray-600 dark:text-gray-400" data-i18n="from $42,910" data-en="from $42,910" data-es="desde $42,910">from $42,910</span>
          </p>
        </div>
      </article>

      <article class="flex flex-col gap-4 rounded-lg border border-gray-200 dark:border-gray-800 bg-white dark:bg-gray-900 p-6">
        <div class="inline-flex gap-2 self-end rounded-sm bg-green-100 dark:bg-green-900/30 p-1 text-green-600 dark:text-green-400">
          <svg aria-hidden="true" xmlns="http://www.w3.org/2000/svg" class="size-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6" />
          </svg>
          <span class="sr-only">Increase: </span>
          <span class="text-xs font-medium" id="dashboard-orders-change">4.1%</span>
        </div>
        <div>
          <strong class="block text-sm font-medium text-gray-600 dark:text-gray-400" data-i18n="Active orders" data-en="Active orders" data-es="Pedidos activos">Active orders</strong>
          <p>
            <span class="text-2xl font-medium text-gray-900 dark:text-white" id="dashboard-orders">2,318</span>
            <span class="text-xs text-gray-600 dark:text-gray-400" data-i18n="from 2,227" data-en="from 2,227" data-es="desde 2,227">from 2,227</span>
          </p>
        </div>
      </article>

      <article class="flex flex-col gap-4 rounded-lg border border-gray-200 dark:border-gray-800 bg-white dark:bg-gray-900 p-6">
        <div class="inline-flex gap-2 self-end rounded-sm bg-red-100 dark:bg-red-900/30 p-1 text-red-600 dark:text-red-400">
          <svg aria-hidden="true" xmlns="http://www.w3.org/2000/svg" class="size-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 17h8m0 0V9m0 8l-8-8-4 4-6-6" />
          </svg>
          <span class="sr-only">Decrease: </span>
          <span class="text-xs font-medium" id="dashboard-customers-change">2.6%</span>
        </div>
        <div>
          <strong class="block text-sm font-medium text-gray-600 dark:text-gray-400" data-i18n="Total customers" data-en="Total customers" data-es="Total clientes">Total customers</strong>
          <p>
            <span class="text-2xl font-medium text-gray-900 dark:text-white" id="dashboard-customers">1,248</span>
            <span class="text-xs text-gray-600 dark:text-gray-400" data-i18n="from 1,216" data-en="from 1,216" data-es="desde 1,216">from 1,216</span>
          </p>
        </div>
      </article>
    </div>

    <div class="grid grid-cols-1 gap-4 lg:grid-cols-3">
      <div class="rounded-lg border border-gray-200 dark:border-gray-800 bg-white dark:bg-gray-900 p-6 lg:col-span-2">
        <div class="flex items-center justify-between">
          <h2 class="text-sm font-medium text-gray-900 dark:text-white" data-i18n="Revenue trend" data-en="Revenue trend" data-es="Tendencia de ingresos">Revenue trend</h2>
          <div class="inline-flex rounded-md border border-gray-200 dark:border-gray-700 p-0.5 text-xs font-medium">
            <button type="button" data-revenue-range="6m" aria-pressed="true" class="rounded-sm bg-gray-100 dark:bg-gray-800 px-2 py-1 text-gray-900 dark:text-white">6M</button>
            <button type="button" data-revenue-range="12m" aria-pressed="false" class="rounded-sm px-2 py-1 text-gray-600 dark:text-gray-400">12M</button>
          </div>
        </div>
        <div class="mt-4 h-64">
          <canvas id="revenue-trend-line-chart" role="img" aria-label="Line chart showing monthly revenue"></canvas>
        </div>
      </div>

      <div class="rounded-lg border border-gray-200 dark:border-gray-800 bg-white dark:bg-gray-900 p-6">
        <h2 class="text-sm font-medium text-gray-900 dark:text-white" data-i18n="Orders by status" data-en="Orders by status" data-es="Pedidos por estado">Orders by status</h2>
        <div class="mt-4 h-64">
          <canvas id="order-status-donut-chart" role="img" aria-label="Donut chart showing orders by status"></canvas>
        </div>
      </div>
    </div>

    <div class="rounded-lg border border-gray-200 dark:border-gray-800 bg-white dark:bg-gray-900 p-6">
      <h2 class="text-sm font-medium text-gray-900 dark:text-white" data-i18n="Recent orders" data-en="Recent orders" data-es="Pedidos recientes">Recent orders</h2>
      <div class="mt-4 overflow-x-auto">
        <table class="min-w-full divide-y-2 divide-gray-200 dark:divide-gray-800">
          <thead class="ltr:text-left rtl:text-right">
            <tr class="*:font-medium *:text-gray-900 dark:*:text-gray-300">
              <th class="px-3 py-2 whitespace-nowrap">Customer</th>
              <th class="px-3 py-2 whitespace-nowrap">Order</th>
              <th class="px-3 py-2 whitespace-nowrap">Status</th>
              <th class="px-3 py-2 whitespace-nowrap">Amount</th>
            </tr>
          </thead>
          <tbody class="divide-y divide-gray-200 dark:divide-gray-800" id="recent-orders-tbody">
          </tbody>
        </table>
      </div>
    </div>

<script src="https://cdn.jsdelivr.net/npm/chart.js@4.5.1/dist/chart.umd.min.js"></script>
<script>
  import { useOrdersStore } from '../lib/stores';

  const isDark = document.documentElement.classList.contains('dark') || localStorage.getItem('theme') === 'dark';
  const textColor = isDark ? 'hsla(220, 9%, 66%, 1)' : 'hsla(220, 9%, 34%, 1)';
  const gridColor = isDark ? 'hsla(217, 33%, 26%, 1)' : 'hsla(220, 13%, 91%, 1)';

  // Populate recent orders from store
  const tbody = document.getElementById('recent-orders-tbody');
  function renderRecentOrders() {
    const orders = useOrdersStore.getState().items;
    const recent = [...orders].reverse().slice(0, 6);
    tbody.innerHTML = recent.map(o => {
      const statusColor = o.status === 'paid' ? 'green' : o.status === 'pending' ? 'yellow' : 'red';
      const statusLabel = o.status === 'paid' ? 'Pagado' : o.status === 'pending' ? 'Pendiente' : 'Reembolsado';
      return `<tr class="*:text-gray-900 dark:*:text-gray-300 *:first:font-medium">
        <td class="px-3 py-2 whitespace-nowrap">${o.customer}</td>
        <td class="px-3 py-2 whitespace-nowrap">${o.number}</td>
        <td class="px-3 py-2 whitespace-nowrap">
          <span class="rounded-full bg-${statusColor}-100 dark:bg-${statusColor}-900/30 px-2.5 py-0.5 text-xs text-${statusColor}-700 dark:text-${statusColor}-400">${statusLabel}</span>
        </td>
        <td class="px-3 py-2 whitespace-nowrap">$${o.amount.toFixed(2)}</td>
      </tr>`;
    }).join('');
  }
  useOrdersStore.subscribe(renderRecentOrders);
  renderRecentOrders();

  // Revenue trend chart
  const revenueTrendLineChartCanvas = document.getElementById('revenue-trend-line-chart') as HTMLCanvasElement;
  const orderStatusDonutChartCanvas = document.getElementById('order-status-donut-chart') as HTMLCanvasElement;
  const revenueRangeButtons = document.querySelectorAll<HTMLButtonElement>('[data-revenue-range]');

  const revenueRangesByPeriod = {
    '6m': { labels: ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun'], values: [28000, 34000, 31000, 39000, 42000, 48000] },
    '12m': { labels: ['Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec', 'Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun'], values: [19000, 21500, 20000, 23500, 25000, 27000, 28000, 34000, 31000, 39000, 42000, 48000] },
  };

  const revenueTrendChartContext = revenueTrendLineChartCanvas.getContext('2d')!;
  const revenueTrendFillGradient = revenueTrendChartContext.createLinearGradient(0, 0, 0, 256);
  revenueTrendFillGradient.addColorStop(0, 'hsla(245, 76%, 58%, 0.25)');
  revenueTrendFillGradient.addColorStop(1, 'hsla(245, 76%, 58%, 0)');

  const revenueTrendLineChart = new Chart(revenueTrendLineChartCanvas, {
    type: 'line',
    data: {
      labels: revenueRangesByPeriod['6m'].labels,
      datasets: [{
        label: 'Revenue', data: revenueRangesByPeriod['6m'].values,
        borderColor: 'hsla(245, 76%, 58%, 1)', backgroundColor: revenueTrendFillGradient,
        borderWidth: 2, pointRadius: 0, pointHoverRadius: 5,
        pointHoverBackgroundColor: 'hsla(245, 76%, 58%, 1)', pointHoverBorderColor: 'hsla(0, 0%, 100%, 1)',
        pointHoverBorderWidth: 2, tension: 0.35, fill: true,
      }],
    },
    options: {
      responsive: true, maintainAspectRatio: false,
      interaction: { mode: 'index', intersect: false },
      plugins: { legend: { display: false }, tooltip: { callbacks: { label: (t: { formattedValue: string }) => `$${t.formattedValue}` } } },
      scales: {
        x: { grid: { display: false }, ticks: { color: textColor } },
        y: { beginAtZero: true, grid: { color: gridColor }, ticks: { color: textColor, callback: (v: number) => `$${v / 1000}k` } },
      },
    },
  });

  revenueRangeButtons.forEach(btn => {
    btn.addEventListener('click', () => {
      const range = btn.dataset.revenueRange!;
      revenueRangeButtons.forEach(b => {
        b.setAttribute('aria-pressed', String(b === btn));
        b.classList.toggle('bg-gray-100', b === btn);
        b.classList.toggle('dark:bg-gray-800', b === btn);
        b.classList.toggle('text-gray-900', b === btn);
        b.classList.toggle('dark:text-white', b === btn);
        b.classList.toggle('text-gray-600', b !== btn);
      });
      revenueTrendLineChart.data.labels = revenueRangesByPeriod[range].labels;
      revenueTrendLineChart.data.datasets[0].data = revenueRangesByPeriod[range].values;
      revenueTrendLineChart.update();
    });
  });

  new Chart(orderStatusDonutChartCanvas, {
    type: 'doughnut',
    data: {
      labels: ['Pagados', 'Pendientes', 'Reembolsados'],
      datasets: [{
        data: (() => {
          const orders = useOrdersStore.getState().items;
          if (orders.length === 0) return [68, 22, 10];
          const paid = orders.filter(o => o.status === 'paid').length;
          const pending = orders.filter(o => o.status === 'pending').length;
          const refunded = orders.filter(o => o.status === 'refunded').length;
          const total = paid + pending + refunded || 1;
          return [Math.round(paid/total*100), Math.round(pending/total*100), Math.round(refunded/total*100)];
        })(),
        backgroundColor: ['hsla(160, 84%, 39%, 1)', 'hsla(36, 93%, 50%, 1)', 'hsla(348, 83%, 55%, 1)'],
        hoverBackgroundColor: ['hsla(160, 92%, 31%, 1)', 'hsla(36, 93%, 44%, 1)', 'hsla(348, 83%, 47%, 1)'],
        borderColor: isDark ? 'hsla(217, 33%, 17%, 1)' : 'hsla(0, 0%, 100%, 1)', borderWidth: 2,
      }],
    },
    options: {
      responsive: true, maintainAspectRatio: false, cutout: '70%',
      plugins: { legend: { position: 'bottom', labels: { color: textColor } } },
    },
  });
</script>
</DashboardLayout>
